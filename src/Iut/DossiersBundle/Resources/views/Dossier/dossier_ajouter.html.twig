{% extends 'IutDossiersBundle::base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block title %} {{ title | capitalize }} {% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-body">
                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                    {{ form_end(form) }}
                    <a href="{{ path('dossier_relancer', {'dossierId': id}) }}" class="btn btn-default">
                        <i class="fa fa-envelope"></i> Relancer
                    </a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}